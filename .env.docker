# Docker Production Environment Variables
# Copy this file to .env.production and configure for your deployment

# Flask Configuration
FLASK_APP=run.py
FLASK_ENV=production

# Security (CRITICAL: Generate a strong random secret key)
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=CHANGE-THIS-TO-A-SECURE-RANDOM-KEY-IN-PRODUCTION

# Database
# For SQLite (default):
# DATABASE_URL is set in docker-compose.prod.yml
# For PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:password@postgres:5432/dozentenmanager
# For MySQL:
# DATABASE_URL=mysql://user:password@mysql:3306/dozentenmanager

# Upload Configuration
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# Logging
LOG_LEVEL=INFO

# Optional: Email configuration (for notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-app-password

# Optional: Backup configuration
# BACKUP_ENABLED=True
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30
